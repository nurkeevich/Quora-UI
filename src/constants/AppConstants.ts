export enum Routes {
    DASHBOARD = "/",
    HELP = "/help",
    LOGIN = "/login",
    SIGNUP = "/signup",
    PROFILE = "/profile",
    CREATEPOST="/createpost"
}

export enum URL {
    GRAPHQL_ENDPOINT = "http://localhost:4000/"
}
