export enum Routes {
    DASHBOARD = "/",
    HELP = "/help",
    LOGIN = "/login",
    SIGNUP = "/signup",
    PROFILE = "/profile"
}

export enum URL {
    GRAPHQL_ENDPOINT = "http://localhost:4000/"
}
